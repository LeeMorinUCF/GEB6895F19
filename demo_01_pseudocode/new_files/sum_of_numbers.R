# Add the elements of a vector

x_list <- c(3,2,6,4,7)

# Initialize
sum <- 0

# Loop through elements
for (i in 1:length(x_list) {
    sum <- sum + x_list[i]
}

# Output to screen
print(sum)


# End
